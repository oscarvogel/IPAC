# IPAC Frontend

Interfaz administrativa inicial para IPAC, construida con Vue 3 y Vite.

## Comandos

```powershell
npm install
npm run dev
npm run build
```

Configurar `VITE_API_BASE_URL` si la API no corre en `http://localhost:8000/api`.
